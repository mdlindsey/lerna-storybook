import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Search from './';

<Meta title="Components|Search/Default" component={Search} />

# Default
<Props of={Search} />

export const rand = () => {
  return Math.random()*9999;
};

With `MDX` we can define a story for `Search` right in the middle of our
markdown documentation.
 
 ## With Variable
<Preview>
  <Story name="with Variable">
    <Search>
      variable {rand()}
    </Search>
  </Story>
</Preview>

## All Search
<Preview>
  <Story name="all Search">
    <Search>
      <span role="img" aria-label="so cool">
        😀 😎 👍 💯
      </span>
    </Search>
  </Story>
</Preview>
